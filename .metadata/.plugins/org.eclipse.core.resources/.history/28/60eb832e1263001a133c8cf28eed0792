spring.application.name=servicio-productos
server.port = ${PORT:0}

eureka.instance.instance-id=${pring.application.name}:${spring.application.instance_id}

eureka.client.service-url.defaultZone=http://localhost:8761/eureka